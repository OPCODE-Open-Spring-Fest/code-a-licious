Problem Statment

Your are given array of integers of N size and a number X. Give maximum sum of subarray of X size.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Input

The first line contains an integer t — the number of test cases.

First line of each test case contains N,X - size of array,size of subarray.
Second line line contains N integers(a[1],a[2].....a[N]).


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Output

For each test case return maximum sum of k size subarray.

-----------------------------------------------------------------------------------------------------------------------------------------------
-------------------------

Constraints:
1 <= N <= 10^5
1 <= a[i] <= 10^5
1 <= X <= N

-----------------------------------------------------------------------------------------------------------------------------------------------
-------------------------

Time limit - 1 sec 
